// CharacterEncoded protocol — tested as potential workaround, not adopted.
// Protocol-based resolution works (like Viewable for View) but adds unnecessary
// protocol overhead. Resolution: use Path.Char / String.Char directly.
